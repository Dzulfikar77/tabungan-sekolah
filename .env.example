# Supabase — client-side (safe to expose, read by the browser bundle)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Supabase — server-side only (scripts/, Supabase Edge Functions).
# NEVER prefix this with VITE_ — that would bundle it into the browser build.
SUPABASE_SERVICE_ROLE_KEY=
